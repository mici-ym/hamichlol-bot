name: CI
on:
    push:
        branches:
            - '**'
    pull_request:
        branches:
            - main

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: npm install
      - run: npm run lint:test
